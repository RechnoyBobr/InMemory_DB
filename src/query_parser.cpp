#include "../inc/query_parser.hpp"
#include <string_view>
#include <vector>

std::vector<memdb::instruction> parse::query_parser::parse(std::string_view) {
  for (int i = 0; i <)
    return std::vector<memdb::instruction>(0);
}
